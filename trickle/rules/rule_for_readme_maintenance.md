When updating the project

- Check if README.md in `trickle/notes` needs updates
- Update README when adding new features or components
- Keep README concise and descriptive
- Include creation/update dates when relevant